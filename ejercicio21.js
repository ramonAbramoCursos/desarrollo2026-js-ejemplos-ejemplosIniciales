/**
 * Pedir por teclado el radio de la base y la altura de un cilindro.
 * Calcular el volumen usando la fórmula: V = π * r² * h
 * Usar Math.PI para el número Pi.
 * Mostrar el resultado con un alert y en el HTML.
 */

// declarar


// constantes que apunten al DOM


// introducir
radio = Number(prompt("Introduce el radio del cilindro:"));
altura = Number(prompt("Introduce la altura del cilindro:"));

// procesar


// mostrar
nodoVolumen.innerHTML = volumen.toFixed(3) + " unidades cúbicas";
