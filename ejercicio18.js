/**
 * Pedir al usuario la temperatura en grados Celsius.
 * Calcular la conversión a Fahrenheit usando la fórmula: 
 * F = (C * 1.8) + 32
 * Mostrar ambos valores en sus respectivos span.
 */

// declarar


// constantes que apunten al DOM


// introducir

// procesar
gradosF = (gradosC * 1.8) + 32;

// mostrar
spanC.innerHTML = gradosC + "°C";
spanF.innerHTML = gradosF + "°F";