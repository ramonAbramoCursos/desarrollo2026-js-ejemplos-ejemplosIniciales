/**
 * Pedir por teclado el precio de un producto (base imponible).
 * Calcular el valor del IVA (21%) y el precio total.
 * 
 * Utilizar el metodo toFixed(2) del objeto Number para redondear a dos decimales
 * Este metodo devuelve un texto
 * numero=2.3456
 * numero.toFixed(2) = "2.35"
 * 
 * Mostrar los tres datos en el HTML dentro de los span que estan dentro de los li
 */

// declarar
let base = 0.0,
    valorIva = 0.0,
    precioFinal = 0.0;

// constantes que apunten al DOM
const spanBase = document.querySelector("#");
const spanIva = document.querySelector("#");
const spanTotal = document.querySelector("#");

// introducir


// procesar


// mostrar
